# Local Instructions (Documentation Scope)

Scope: files under `documentation/`.

This file replaces the former `documentation/AGENTS.md` location so there is a
single `AGENTS.md` at repository root.

Guidance:
- Treat `AGENTS.md` at repo root as the authoritative instruction source.
- Keep documentation consistent with root invariants and state-machine rules.
- Avoid introducing guidance in docs that conflicts with root `AGENTS.md`.
