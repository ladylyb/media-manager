# Installation

## Prerequisites

- Python 3.11+
- Git

## Setup

```bash
python -m venv .venv
source .venv/bin/activate
pip install -e ".[dev]"
```

For documentation tooling:

```bash
pip install -e ".[docs]"
```
